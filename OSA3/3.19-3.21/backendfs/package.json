{
  "name": "notebackend",
  "version": "1.0.0",
  "description": "",
  "main": " dist/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd C:/Users/Hoda%20Hussein/frontendFS && npm run build && cp -r dist C:/Users/Hoda%20Hussein/backendFS",
    "deploy:full": "npm run build:ui && cd C:/Users/Hoda%20Hussein/backendFS && git add . && git commit -m 'uibuild' && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "mongoose": "^8.11.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.0",
    "nodemon": "^3.0.3"
  }
}
